# R50: 阶段A出段综合大考：所有权三大定律、借用检查器与生命周期

> **考核定位**：阶段 A 30 讲全景因果闭环考核 · 所有权 Move · 借用检查器 · 生命周期 NLL 与 Polonius

---

## 1. 出段大考说明

本节点为 **Rust 系统级第一性原理大系 阶段 A 出段判据**。
全面考核仿射类型系统、Drop 抑制机制（`ManuallyDrop`）、`noalias` 性能优化与 Polonius Datalog 求解模型。

---

## 2. 核心考点矩阵

1. **Move 语义与底层机器码**：
   - 小结构体寄存器直接传递；
   - SROA 标量替换消除冗余拷贝；
   - Drop Flags 栈标志位动态控制。
2. **借用检查器与生命周期**：
   - 共享借用只读协变 vs 独占借用读写不变；
   - Universal Regions 泛型签名占位符；
   - Polonius Loan 路径敏感生命周期。
